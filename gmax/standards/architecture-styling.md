# Styling & conventions

<!-- How the code looks: formatting, naming, styling system, forbidden
     patterns. Keep it enforceable — rules an agent can check, not taste. -->

## Naming

## Styling system

<!-- Design tokens / theme source of truth, if any. State the rule:
     e.g. "no hardcoded values — tokens only". -->

## Forbidden patterns

<!-- e.g. no default exports, no direct console.log, no style fallbacks -->
