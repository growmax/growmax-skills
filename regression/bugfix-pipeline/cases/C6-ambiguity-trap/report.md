The team summary page and the rep's own dashboard both have a card titled "Revenue", but they show
completely different numbers — 1110 on the summary, 10 on the dashboard, same rep, same month.
Two cards with the same name showing different things is confusing. Please fix the label situation.

App is at <SANDBOX PATH>. Cards: `src/summary.js` and `src/dashboard.js`; titles come from
`locales/en.json` via `src/labels.js`.
